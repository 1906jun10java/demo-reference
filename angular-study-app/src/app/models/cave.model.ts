export class Cave {
    id: number;
    name: string;
    maxBears: number;

    constructor(id: number, name: string, maxBears: number) {
        this.id = id;
        this.name = name;
        this.maxBears = maxBears;
    }
}
